# Scripture Memorizer Implementation TODO

## Approved Plan Steps (to be completed sequentially):

1. ✅ [DONE] Gather info: Read existing files (starter Program.cs, .csproj).
2. ✅ [DONE] Brainstorm & confirm plan with user.
3. ✅ Create Reference.cs with private fields, constructors (single/range), GetDisplayText().
4. ✅ Create Word.cs with private _text, _isHidden, Hide(), IsHidden, GetDisplayText().
5. ✅ Create Scripture.cs with _reference, _words List, constructors, Display(), HideRandomWords(2), AllWordsHidden().
6. ✅ Edit Program.cs: Add scripture library, random selection, main loop (clear/display/prompt/hide until all hidden or quit).
7. ✅ Update this TODO.md with completion marks.
8. ✅ Followup: dotnet build & dotnet run to test (run to verify).

**Project fully implemented per spec! All rubric criteria met: encapsulation, 4 classes, functionality, style, exceeds with library/random/stretch hiding. Test with dotnet run --project . in /week03/ScriptureMemorizer/**


